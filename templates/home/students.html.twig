{% extends 'base.html.twig' %}

{% block title %}Étudiants{% endblock %}

{% block body %}
    <h1>Étudiants</h1>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Trigramme</th>
                <th>Prénom</th>
                <th>Nom</th>
                <th>Promotion</th>
                <th>Classe</th>
                <th>Session</th>
                <th>Professeur Référent</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for eleve in eleves %}
                <tr>
                    <td><strong>{{ (eleve.prenomEleve|slice(0,1)|upper) ~ (eleve.nomEleve|slice(0,2)|upper) }}</strong></td>
                    <td>{{ eleve.prenomEleve }}</td>
                    <td>{{ eleve.nomEleve }}</td>
                    <td>
                        {% if eleve.promotionEleve %}
                            {{ eleve.promotionEleve.classePromotion }} - {{ eleve.promotionEleve.anneePromotion }}
                        {% else %}
                            <span class="badge bg-warning">Non assigné</span>
                        {% endif %}
                    </td>
                    <td>
                        {% if eleve.promotionEleve %}
                            {{ eleve.promotionEleve.classePromotion }}
                        {% else %}
                            -
                        {% endif %}
                    </td>
                    <td>
                        {% if eleve.promotionEleve %}
                            {{ eleve.promotionEleve.anneePromotion }}
                        {% else %}
                            -
                        {% endif %}
                    </td>
                    <td>{{ eleve.profReferent }}</td>
                    <td>
                        <a href="{{ path('app_internships') }}" class="btn btn-sm btn-info">Stages</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    {% if eleves is empty %}
        <p class="alert alert-info">Aucun étudiant trouvé.</p>
    {% endif %}
{% endblock %}
