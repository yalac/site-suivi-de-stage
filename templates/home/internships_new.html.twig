{% extends 'base.html.twig' %}

{% block title %}Nouveau stage{% endblock %}

{% block body %}
    <h1>Créer un stage</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Enregistrer</button>
    {{ form_end(form) }}

    <p><a href="{{ path('app_internships') }}">Retour à la liste</a></p>
{% endblock %}
